// JS Objects are a set of key value pairs

// Js Object with 3 members -> one property and two methods
const person = {
    name: "Allen",
    walk() {}, // added function in an Object is called a method
    talk() {}
}

// two ways to access members

// the dot notation
person.name = "Steven"

// the bracket notation
person['name'] = "Steven"